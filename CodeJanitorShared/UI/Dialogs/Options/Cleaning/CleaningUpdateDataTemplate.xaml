<<<<<<< HEAD
<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:converters="clr-namespace:CodeJanitor.UI.Converters"
	xmlns:enums="clr-namespace:CodeJanitor.UI.Enumerations"
	xmlns:local="clr-namespace:CodeJanitor.UI.Dialogs.Options.Cleaning"
	xmlns:p="clr-namespace:CodeJanitor.Properties"
	xmlns:ui="clr-namespace:CodeJanitor.UI">
	<DataTemplate DataType="{x:Type local:CleaningUpdateViewModel}">
		<StackPanel>
			<GroupBox Header="Code Style">
				<StackPanel>
					<CheckBox Content="{x:Static p:Resources.UpdateAccessorsToBothBeSingleLineOrMultiLineSA1504}" IsChecked="{Binding UpdateAccessorsToBothBeSingleLineOrMultiLine}" />
					<CheckBox Content="{x:Static p:Resources.UpdateEndregionTagsWithRegionName}" IsChecked="{Binding UpdateEndRegionDirectives}" />
					<TextBlock Margin="35,0,5,10" Text="{x:Static p:Resources.ExEndregionMethods}" />
					<CheckBox Content="{x:Static p:Resources.UpdateSingleLineMethodsByPlacingBracesOnSeparateLinesSA1502}" IsChecked="{Binding UpdateSingleLineMethods}" />
					<CheckBox Content="Convert block-scoped namespace to file-scoped (C# 10+)" IsChecked="{Binding ConvertToFileScopedNamespace}" />
					<CheckBox Content="Move extra top-level C# types into their own files (skip partial, nested, and non-region preprocessor cases)" IsChecked="{Binding MoveTopLevelTypesToSeparateFiles}" />
					<CheckBox Content="Convert local variables to 'var' when the type is apparent" IsChecked="{Binding ConvertToVarWhenApparent}" />
					<CheckBox Content="Convert List&lt;T&gt;/array initializations to collection expressions ([] / [a, b, c])" IsChecked="{Binding ConvertToCollectionExpressions}" />
					<CheckBox Content="CA1869: replace direct JsonSerializerOptions allocations in JsonSerializer calls with null (conservative)" IsChecked="{Binding ReuseJsonSerializerOptionsForCA1869}" />
					<CheckBox Content="Simplify single-statement lambda blocks to expression lambdas (remove braces/return)" IsChecked="{Binding SimplifySingleStatementLambdas}" />
					<CheckBox Content="Add 'readonly' to fields never written outside their constructor" IsChecked="{Binding MakeFieldsReadonlyWhenSafe}" />
					<CheckBox Content="Add 'sealed' to classes not derived from in the same file (single-file heuristic)" IsChecked="{Binding SealClassesWhenSafe}" />
					<CheckBox Content="Insert a blank line before 'return' and 'throw' statements that end a block of logic" IsChecked="{Binding InsertBlankLineBeforeReturnAndThrowStatements}" />
					<CheckBox Content="Format Razor components (.razor) in safe mode" IsChecked="{Binding FormatRazorComponents}" />
					<StackPanel Orientation="Horizontal">
						<TextBlock VerticalAlignment="Center" Text="Razor attribute inline threshold" />
						<TextBox
							Width="40"
							Text="{Binding RazorAttributeWrapThreshold, UpdateSourceTrigger=PropertyChanged}"
							TextAlignment="Right" />
						<TextBlock
							Margin="6,0,0,0"
							VerticalAlignment="Center"
							Text="(0 = use default 2)" />
					</StackPanel>

					<Separator Margin="0,8,0,8" />
					<TextBlock FontWeight="SemiBold" Text="AI-assisted XML documentation (C# methods)" />
					<CheckBox
						Content="Enable AI-assisted XML documentation"
						IsChecked="{Binding AiXmlDocumentationEnabled}"
						IsEnabled="{Binding IsEnabledAiXmlDocumentationEnabled}" />
					<CheckBox
						Margin="0,4,0,0"
						Content="Run AI-assisted XML documentation during code cleanup"
						IsChecked="{Binding AiXmlDocumentationRunDuringCleanup}"
						IsEnabled="{Binding IsEnabledAiXmlDocumentationRunDuringCleanup}" />
=======
<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:p="clr-namespace:CodeJanitor.Properties"
        xmlns:local="clr-namespace:CodeJanitor.UI.Dialogs.Options.Cleaning"
        xmlns:ui="clr-namespace:CodeJanitor.UI"
        xmlns:converters="clr-namespace:CodeJanitor.UI.Converters"
        xmlns:enums="clr-namespace:CodeJanitor.UI.Enumerations">
    <DataTemplate DataType="{x:Type local:CleaningUpdateViewModel}">
        <StackPanel>
            <GroupBox Header="Code Style">
                <StackPanel>
                    <CheckBox Content="{x:Static p:Resources.UpdateAccessorsToBothBeSingleLineOrMultiLineSA1504}" IsChecked="{Binding UpdateAccessorsToBothBeSingleLineOrMultiLine}" />
                    <CheckBox Content="{x:Static p:Resources.UpdateEndregionTagsWithRegionName}" IsChecked="{Binding UpdateEndRegionDirectives}" />
                    <TextBlock Text="{x:Static p:Resources.ExEndregionMethods}" Margin="35,0,5,10" />
                    <CheckBox Content="{x:Static p:Resources.UpdateSingleLineMethodsByPlacingBracesOnSeparateLinesSA1502}" IsChecked="{Binding UpdateSingleLineMethods}" />
                    <CheckBox Content="Convert block-scoped namespace to file-scoped (C# 10+)" IsChecked="{Binding ConvertToFileScopedNamespace}" />
                    <CheckBox Content="Move extra top-level C# types into their own files (skip partial, nested, and non-region preprocessor cases)" IsChecked="{Binding MoveTopLevelTypesToSeparateFiles}" />
                    <CheckBox Content="Convert local variables to 'var' when the type is apparent" IsChecked="{Binding ConvertToVarWhenApparent}" />
                    <CheckBox Content="Convert List&lt;T&gt;/array initializations to collection expressions ([] / [a, b, c])" IsChecked="{Binding ConvertToCollectionExpressions}" />
                    <CheckBox Content="CA1869: replace direct JsonSerializerOptions allocations in JsonSerializer calls with null (conservative)" IsChecked="{Binding ReuseJsonSerializerOptionsForCA1869}" />
                    <CheckBox Content="Simplify single-statement lambda blocks to expression lambdas (remove braces/return)" IsChecked="{Binding SimplifySingleStatementLambdas}" />
                    <CheckBox Content="Add 'readonly' to fields never written outside their constructor" IsChecked="{Binding MakeFieldsReadonlyWhenSafe}" />
                    <CheckBox Content="Add 'sealed' to classes not derived from in the same file (single-file heuristic)" IsChecked="{Binding SealClassesWhenSafe}" />
                    <CheckBox Content="Insert a blank line before 'return' and 'throw' statements that end a block of logic" IsChecked="{Binding InsertBlankLineBeforeReturnAndThrowStatements}" />
                    <CheckBox Content="Format Razor components (.razor) in safe mode" IsChecked="{Binding FormatRazorComponents}" />

                    <Separator Margin="0,8,0,8" />
                    <TextBlock Text="AI-assisted XML documentation (C# methods)" FontWeight="SemiBold" />
                    <CheckBox Content="Enable AI-assisted XML documentation during cleanup"
                                  IsChecked="{Binding AiXmlDocumentationEnabled}"
                                  ToolTip="Requires a successful 'Test Connection' before it can be turned on; can always be turned off." />
>>>>>>> b9e78414af282a58c367e7d5c92e87b209aead52

					<TextBlock Margin="0,4,0,0" Text="Endpoint URL" />
					<TextBox Text="{Binding AiXmlDocumentationEndpointUrl, UpdateSourceTrigger=PropertyChanged}" />

					<TextBlock Margin="0,4,0,0" Text="API key" />
					<PasswordBox ui:PasswordBoxAssistant.BindPassword="True" ui:PasswordBoxAssistant.BoundPassword="{Binding AiXmlDocumentationApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

					<TextBlock Margin="0,4,0,0" Text="API key header (Authorization / api-key / x-api-key)" />
					<TextBox Text="{Binding AiXmlDocumentationApiKeyHeader, UpdateSourceTrigger=PropertyChanged}" />

					<TextBlock Margin="0,4,0,0" Text="Model or deployment name (optional)" />
					<TextBox Text="{Binding AiXmlDocumentationModel, UpdateSourceTrigger=PropertyChanged}" />

					<StackPanel Margin="0,4,0,0" Orientation="Horizontal">
						<TextBlock VerticalAlignment="Center" Text="Timeout (seconds)" />
						<TextBox
							Width="48"
							Margin="8,0,0,0"
							Text="{Binding AiXmlDocumentationTimeoutSeconds, UpdateSourceTrigger=PropertyChanged}"
							TextAlignment="Right" />
						<TextBlock
							Margin="12,0,0,0"
							VerticalAlignment="Center"
							Text="Max methods/file" />
						<TextBox
							Width="48"
							Margin="8,0,0,0"
							Text="{Binding AiXmlDocumentationMaxMethodsPerFile, UpdateSourceTrigger=PropertyChanged}"
							TextAlignment="Right" />
						<Button
							Margin="12,0,0,0"
							Padding="10,2"
							Command="{Binding TestAiXmlDocumentationConnectionCommand}"
							Content="Test Connection" />
					</StackPanel>

<<<<<<< HEAD
					<CheckBox
						Margin="0,4,0,0"
						Content="Preview AI XMLDoc changes before applying per file"
						IsChecked="{Binding AiXmlDocumentationPreviewChanges}" />
					<CheckBox Content="Allow deterministic fallback when AI response fails" IsChecked="{Binding AiXmlDocumentationAllowDeterministicFallback}" />
=======
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <TextBlock Text="Minimum context window (tokens)" VerticalAlignment="Center"
                                       ToolTip="Recommended context size for the model to reliably process a full method's context (default 131072). For local endpoints (e.g. Ollama, llama.cpp), the actual context size must usually still be configured on the server itself (e.g. Ollama Modelfile 'PARAMETER num_ctx'); the OpenAI-compatible chat API does not let clients set it directly." />
                            <TextBox Text="{Binding AiXmlDocumentationContextWindowTokens, UpdateSourceTrigger=PropertyChanged}" Width="64" Margin="8,0,0,0" TextAlignment="Right" />
                        </StackPanel>

                    <CheckBox Content="Preview AI XMLDoc changes before applying per file" IsChecked="{Binding AiXmlDocumentationPreviewChanges}" Margin="0,4,0,0" />
                    <CheckBox Content="Allow deterministic fallback when AI response fails" IsChecked="{Binding AiXmlDocumentationAllowDeterministicFallback}" />
>>>>>>> b9e78414af282a58c367e7d5c92e87b209aead52

					<StackPanel Margin="0,4,0,0" Orientation="Horizontal">
						<TextBlock VerticalAlignment="Center" Text="Max requests/cleanup" />
						<TextBox
							Width="48"
							Margin="8,0,0,0"
							Text="{Binding AiXmlDocumentationMaxRequestsPerCleanup, UpdateSourceTrigger=PropertyChanged}"
							TextAlignment="Right" />
						<TextBlock
							Margin="12,0,0,0"
							VerticalAlignment="Center"
							Text="Max input chars/method" />
						<TextBox
							Width="64"
							Margin="8,0,0,0"
							Text="{Binding AiXmlDocumentationMaxInputCharsPerMethod, UpdateSourceTrigger=PropertyChanged}"
							TextAlignment="Right" />
					</StackPanel>

					<StackPanel Margin="0,4,0,0" Orientation="Horizontal">
						<TextBlock VerticalAlignment="Center" Text="Max output tokens/request" />
						<TextBox
							Width="48"
							Margin="8,0,0,0"
							Text="{Binding AiXmlDocumentationMaxTokensPerRequest, UpdateSourceTrigger=PropertyChanged}"
							TextAlignment="Right" />
						<TextBlock
							Margin="12,0,0,0"
							VerticalAlignment="Center"
							Text="Max estimated tokens/cleanup" />
						<TextBox
							Width="64"
							Margin="8,0,0,0"
							Text="{Binding AiXmlDocumentationMaxEstimatedTokensPerCleanup, UpdateSourceTrigger=PropertyChanged}"
							TextAlignment="Right" />
						<TextBlock
							Margin="12,0,0,0"
							VerticalAlignment="Center"
							Text="Global timeout (s)" />
						<TextBox
							Width="48"
							Margin="8,0,0,0"
							Text="{Binding AiXmlDocumentationGlobalTimeoutSeconds, UpdateSourceTrigger=PropertyChanged}"
							TextAlignment="Right" />
					</StackPanel>

					<CheckBox
						Margin="0,4,0,0"
						Content="Ignore [GeneratedCode]/[CompilerGenerated] methods"
						IsChecked="{Binding AiXmlDocumentationIgnoreGeneratedCode}" />
					<CheckBox Content="Ignore [Obsolete] methods" IsChecked="{Binding AiXmlDocumentationIgnoreObsolete}" />
					<CheckBox Content="Ignore unit test methods (Fact/Theory/TestMethod/etc.)" IsChecked="{Binding AiXmlDocumentationIgnoreTestMethods}" />
					<TextBlock Margin="0,4,0,0" Text="Ignore regex (full member path Namespace.Type.Method)" />
					<TextBox Text="{Binding AiXmlDocumentationIgnorePattern, UpdateSourceTrigger=PropertyChanged}" />

					<TextBlock
						Margin="0,4,0,0"
						FontStyle="Italic"
						Text="{Binding AiXmlDocumentationConnectionStatus}"
						TextWrapping="Wrap" />
				</StackPanel>
			</GroupBox>
			<GroupBox Header="{x:Static p:Resources.FileHeader}">
				<StackPanel>
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition />
							<RowDefinition />
						</Grid.RowDefinitions>
						<RadioButton
							Grid.Row="0"
							Content="Insert"
							GroupName="HeaderMode"
							IsChecked="{Binding HeaderUpdateMode, Converter={x:Static converters:EnumToBooleanConverter.Default}, ConverterParameter={x:Static enums:HeaderUpdateMode.Insert}}"
							ToolTip="Inserts a new header if the one already present is different. If no header is present, the defined header is simply inserted." />
						<RadioButton
							Grid.Row="1"
							Content="Replace"
							GroupName="HeaderMode"
							IsChecked="{Binding HeaderUpdateMode, Converter={x:Static converters:EnumToBooleanConverter.Default}, ConverterParameter={x:Static enums:HeaderUpdateMode.Replace}}"
							ToolTip="Replaces the existing header. If no header is present, the defined header is simply inserted." />
					</Grid>
					<TabControl Margin="0,5">
						<TabItem Header="C#">
							<StackPanel Orientation="Vertical">
								<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderCSharp, UpdateSourceTrigger=PropertyChanged}" />
								<GroupBox Margin="5,0,5,5" Header="Position">
									<Grid>
										<Grid.RowDefinitions>
											<RowDefinition />
											<RowDefinition />
										</Grid.RowDefinitions>
										<RadioButton
											Grid.Row="0"
											Content="Start of document"
											GroupName="HeaderInsertStyle"
											IsChecked="{Binding HeaderPosition, Converter={x:Static converters:EnumToBooleanConverter.Default}, ConverterParameter={x:Static enums:HeaderPosition.DocumentStart}}" />
										<RadioButton
											Grid.Row="1"
											Content="After usings"
											GroupName="HeaderInsertStyle"
											IsChecked="{Binding HeaderPosition, Converter={x:Static converters:EnumToBooleanConverter.Default}, ConverterParameter={x:Static enums:HeaderPosition.AfterUsings}}" />
									</Grid>
								</GroupBox>
							</StackPanel>
						</TabItem>
						<TabItem Header="C++">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderCPlusPlus, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="F#">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderFSharp, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="VB">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderVisualBasic, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="PHP">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderPHP, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="PowerShell">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderPowerShell, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="R">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderR, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="JSON">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderJSON, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="XAML">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderXAML, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="XML">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderXML, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="HTML">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderHTML, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="CSS">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderCSS, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="LESS">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderLESS, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="SCSS">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderSCSS, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="JavaScript">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderJavaScript, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
						<TabItem Header="TypeScript">
							<TextBox Style="{DynamicResource TextBoxMultiLine}" Text="{Binding UpdateFileHeaderTypeScript, UpdateSourceTrigger=PropertyChanged}" />
						</TabItem>
					</TabControl>
				</StackPanel>
			</GroupBox>
		</StackPanel>
	</DataTemplate>
</ResourceDictionary>
